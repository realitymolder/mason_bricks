name: atomic_CSR_feature
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: '>=0.1.0-dev.26 <0.1.0'

vars:
  feature:
    type: string
    description: Feature Name
    default: Auth
    prompt: What is the new feature name?
